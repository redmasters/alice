spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: ${ALICE_H2_URL}
    username: sa
    password:
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update
