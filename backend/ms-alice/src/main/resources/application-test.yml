spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: ${ALICE_H2_URL}
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update
